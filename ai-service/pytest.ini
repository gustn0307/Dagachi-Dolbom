[pytest]

# ai-service 프로젝트 루트를 Python import 경로에 추가합니다.
# 테스트 코드에서 `from app...` 형태의 import를 안정적으로 사용할 수 있습니다.
pythonpath = .

# tests 폴더 아래의 test_*.py 파일을 테스트 대상으로 수집합니다.
testpaths = tests

# 테스트 실행 결과를 조금 더 읽기 쉽게 표시합니다.
addopts = -ra